# Integrity notice — esim_phase3b4.md is INCOMPLETE

The eSIM Phase 3B4 source prompt was delivered inside a chat message that hit a
50,000-character limit and was cut off. The stored copy therefore ends mid-sentence:

    If the maturity gate has not been reached, complete the safe

Everything above that line is byte-exact as received. Nothing has been paraphrased,
completed or invented — completing it would fabricate instructions the owner never gave.

The `esim_phase3b4_20260901` run must not execute against this copy. Supply the
complete source prompt, re-run `install_measurement_queue.sh`, and the manifest
SHA-256 will change to reflect the authoritative version.
